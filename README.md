This document is going to be a writeup of every single piece of code in the Chirpy REST api. To preface the rest of this document I would like to introduce this backend api from a higher level. This api essentially mimics the back end of a social media platform like twitter. Users can create accounts, login to them, post chirps aka tweets, delete these tweets and other misc features. The main technologies used in this project apart from go are postgres for the db which stores all the info needed, sqlc for interacting with the backend db via our go code, and JWT tokens used for auth.

Now to begin dissecting this backend api, I will start from the main function line by line and breakdown each endpoint and its handler logic, along the way I will also explain the sql queries used in the handlers and other functionalities. I will also assume the person reading this (if any) are following along with the code themselves.

To start on the main function, we first load the enviroment variables located in the root of the project under .env. This is not commited to github for obvious reasons.
Also I will not be stating the if err != nil each time as this will take so long just assume each time a err is returned were properly handling it. 
Then we load each env variable via its key name storing the value in a variable.

We then use our imported sql library and drivers to open a handle to our db via our db url loaded in from env vars. We defer this db handle to be closed after. We open a new dbquery struct passing in this handle. This db query struct is what allows us to run the queries we have for our db via our go code the queries struct returned from sqlc exposes methods which when executed run the specific queries we set. All in all sqlc finds the .sql files we have saved in a dir reads them makes methods in our go code on this queries struct we can use and when ran it runs those queries boom.
We then make our apiconfig, this will store info we need for our handlers later on as well as this queries struct we just made. We then create a new serve mux. This serve mux essentially will act as a router for our endpoints, we pass in a pattern it should match for exp /api/users and then a handler of httphandler type. This is an interface type which makes sure the type passed in has a serve http method which takes in a response writer and the request. The response writer is a interface implemented on a hidden struct in the net/http lib we use. It is passed to us via the server we define and allows us to use the given methods in the interface to construct http responses and send them. The request is a pointer to a struct reprsenting the request our http servert got allowing us to fetch data from it and comes built in with methods we can use on it to perform certain actions or fetch certain data. So now that we broke down the srv mux and what it does (calls handlers based on specific endpoints hit) we can go over each handler. Also i Apolagixze if somebody is reading this im not going to fix the grammar or etc im more so writing this so i can solidify my understanding of the backend server project. Our first three endpoints and handlers are all user related.
the server mux has a .handle method which takes in the endpoint and a specific method for the endpoint and the handler. This gives us some flexivbility for exp if we want diff handlers for each method on one endpoint yeah this lets us do that. 
So the first one is make user. Btw all of these handlers pretty much are a method of our api struct we made as this gives them direct access to the db handle and queries.
So first handler is responsible for POSt/api/users it makes a user !!. In the make user handler we return a http.handler interface closure function like ill show u rq:
```go
func (cfg *ApiConfig) MakeUser() http.Handler {

    return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
```
we do this so we can still have a pointer to the api config but also be of the handler func interface type which the srv mux expects. Because lets say we were to just not do this and make a function which takes in a pointer to a api config this would break the server mux would not be chill with that. Then we decode the request into a struct of the json body shape of the request. we hash the password given using a argon2id library. And then we call our db handler query struct with the create user query passing in all required params whicch are just email and hashed pass. The query is just a insert into table lmao nothing special. Then we call our respond with json function on the response struct created type shape. Our respond with json simply Sets the header content typeand passed in header code marshals the given struct and calls w.write. Btw the response writer doesnt need a pointer like the request since its an interface, an interface points to the original mem location of the struct via a pointer so even tho its copied its still pointing. next we have update user. Decodes req, uses get bearer token on the header, this simply fetches the auth header splits it and returns the api key value if properly made. This update user endpoint expects a jwt as u need a way to verify ur who u say u are and also which uset to update so its a 2 in one type ordeal. We call validate jwt on the jwt given via headers. This function calls parse with claims, it checks if the supplied jwt is not expired matches our jwt secret to verify we made this jwt token and it supplies a claims struct which returns the user id who owns this jwt. we hash the new pass they want to update to, and update the user which is just a simply update table set columns where id = passed id. we ofc pass in the new email and pass and the id of the user we wanna update. then we send the response via our respond with json again. Next we have user login.
This decodes the req, gets the user row via email as this is what they use to sign in, we then take the password and compare it with that users hashed password the one they are trying to log in to via the email if wrong pass or wrong email but right pass for another account we just return wrong email or pass. We then make a refresh token for the logged in user, this is just a random 32 bit value generated. The purpose of this is that, periodically every hour when the jwt expires we will use this refresh token which expires every 60 days to auto regenerate the jwt token for the user. The web browser should do this automatically. This is done for two reasons, One so the user doesnt have to relogin in every hour and make a new jwt and two so that if somehow the users jwt gets stolen we can refresh that users jwt. SO then we insert that refresh token into a table with the user id so later when we need to refresh the jwt we can see who the passed in refresh token is linked to. Then we make the jwt This function simply makes the claims which is the issuer subject aka the user id the exp and we sign it with the secret. Then we return all the user info we have from the db and the access and refresh token. NExt we have all the chirp endpoints. Lets start from the post chirp end point. decodes the req into the body we weexpect checks len if too long rejects we filter for bad words thi is done using a map of the bad words we split the body of the tweet we lower each string if it lowered is a key we filtert it if not we add it and return that final clean string.  We get the berarer token from the poster, we validate its a valid jwt not expired correct secret and get the id in the jwt, we create the chirp which is just the body and user id, and send the full chirp info back to the reciever. Next we have get chirp which supports query params and get chirp no query params but can specifiy a specific chirp it wants via its uuid, The all chirps with query params first grabs the params if present gets all chirps from db, if it has an author id we parse it as a uuid, Then if it had that by author id we return in the final list of chirps only the ones with that id, 
Then if it wanted to sort on asc or descending we use: 
```go
sort.Slice(chirps, func(i, j int) bool {
    if sortDirection == "desc" {
        return chirps[i].CreatedAt.After(chirps[j].CreatedAt)
    }
    return chirps[i].CreatedAt.Before(chirps[j].CreatedAt)
})
```
basically the logic is itll ask do u want i before j for descending if i is created after j that would mean it would return true so imagine u have 3 , 1, 2 should 3 come before 1 because were filtering it out on created after the other value that would be true so we dont swap then 1 and 2 its false so we swap. For asc the logic would be reverse so 3, 1, 2 would be like was 3 created before 1 no so ygwueduewyhdbauyhdb yeah u  get it its whatevrr beor then we marshal that and send it back. GET CHIRP DOES THE SAME THING BUITTTT, it only returns the chirp specified in the query params. NOTHING ELSE> DELETE CHIRP JUST gets the id from the query, gets that chirp, gets the id from the jwt if the id matches the chirps owner id we delete if not we err. The webhook api endpoint is essentially caled with a secret api key when we should upgrade a user is supposed to like mimic stripe and sends a webhoojk to us telling us we shouldupgrade this user, we decode if not the evewnt we expect return 200 meaning its ok dont send again if it doesnt get a 200 back itll keep sending us requests so yeah, we get the api key if it matches the one we want we parse the user id were upgrading, then we add the is upgraded true flag to his row and write that were done. The next two pertain to the refresh token, We get the refresh token from headers, we get the user for this token, if its revoked we just return an error as u can revoke a refresh token, then we simply just make a new jwt for the user with their id.
Revoke does the same thing with like fetching from headerts but it deletes the token by just marking revoked = true on its row. We also have a file server running in the root dir well it thinks its the root but its really just the /app/and anything mapped after that will just be what is in our file dir and itll fetch it.  yk what this doc is alr 10k chars long so yheah thats all the main functionality everything else is boring and i alr get it anyways SO GG IF U READ KISS ME>

